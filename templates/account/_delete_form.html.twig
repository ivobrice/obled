<form method="post" action="{{ path('app_account_delete') }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer votre compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('deleteCompte' ~ user.id) }}">
    <button class="btn btn-primary btn-lg btn-block" formnovalidate>Supprimer mon compte</button>
</form>
