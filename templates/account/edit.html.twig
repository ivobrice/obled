{% extends 'base.html.twig' %}

{% block title %}Modifier mes informations{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1 class="h3 my-5 font-weight-normal text-center">Modifier mes informations</h1>

            {{ include('userForm/_form.html.twig', {'button_label': 'Modifier'}) }}

            <p><a href="{{ path('app_account_show') }}" class="text-center">Voir mes informations</a></p>

            <p>{{ include('account/_delete_form.html.twig') }}</p>
        </div>
    </div>
{% endblock %}
